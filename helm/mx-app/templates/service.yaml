apiVersion: v1
kind: Service
metadata:
  name: {{ include "mx-app.fullname" . }}
  labels:
    {{- include "mx-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mx-app.selectorLabels" . | nindent 4 }}
