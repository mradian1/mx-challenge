apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mx-app.fullname" . }}-test-connection"
  labels:
    {{- include "mx-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mx-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
